# SSTP server setup

/interface sstp-server server
  set enabled=yes certificate=server verify-client-certificate=yes

/ppp secret
  remove [ find service=sstp ]
  add service=sstp name={{ sstp_connection.username }} password={{ sstp_connection.password }} \
    local-address=192.168.100.11 remote-address=192.168.100.12
